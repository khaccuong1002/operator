inputWidth = prompt("inputWidth");
inputHeight = prompt( "let inputHeight");
let width = parseInt(inputHeight);
let height = parseInt(inputHeight);
let area = width * height;
document.write("The area is: " + area);